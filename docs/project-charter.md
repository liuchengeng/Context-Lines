# Product Charter

ContextLines Quick Ask 帮助用户在看已有字幕的视频时快速弄懂刚才听到的短语或俚语。

核心流程只有五步：点击扩展开始监听、播放视频、按 `Alt+Q`、查看右上角简短解释、点 `×` 或再按快捷键关闭。问答不暂停视频、不改变窗口焦点，也不退出网页全屏。

首版不做持续字幕、侧栏、收藏、复习、登录、网页字幕读取、OCR、人物识别或历史记录。音频仅保存在内存循环缓冲区；用户按快捷键后才将最近约 10 秒发送给问答 Worker。扩展和 Worker 都不持久化音频或转写。
