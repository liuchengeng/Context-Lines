# Project Plan

- 完成：Chrome 离屏音频捕获、10 秒内存缓冲、`Alt+Q` 和全屏内非模态解释卡片。
- 完成：扩展设置页、最近音频捕获和非模态解释卡片。
- 完成：旧版豆包流式识别小时版协议与 DeepSeek 结构化解释。
- 完成：用 Cloudflare Worker 中转豆包 WebSocket，并把模型密钥从 Chrome 移到 Worker Secrets。
- 待用户授权：创建并部署 Worker，然后在真实视频上验证豆包与 DeepSeek 响应。

不创建远程资源、不部署、不推送。
