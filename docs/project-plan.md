# Project Plan

当前目标是交付未部署的 Chrome 116+ 轻量 MVP。

- 完成：离屏标签页音频捕获、10 秒内存缓冲、`Alt+Q`、全屏内非模态解释卡片、mock 流程。
- 完成：`POST /v1/quick-ask` 契约，以及 Qwen ASR 到 DeepSeek 解释的服务端链路。
- 验证：类型检查、单元测试、构建和扩展清单。
- 待真实账号：配置 DashScope、DeepSeek、稳定扩展 ID 与 Cloudflare secrets 后进行真实端到端验收。

不创建远程资源、不部署、不推送。
